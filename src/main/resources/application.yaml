server:
  port: 8080
  servlet:
    context-path: /ProFitSuppsDB

spring:
  datasource:
    url: "jdbc:mysql://localhost:3306/ProFitSuppsDB"
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true